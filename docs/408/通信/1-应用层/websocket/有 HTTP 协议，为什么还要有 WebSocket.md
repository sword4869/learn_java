# 有 HTTP 协议，为什么还要有 WebSocket

TCP 协议本身是**全双工**的，但 基于TCP的 HTTP/1.1 是请求应答模式，导致它是**半双工**的。

​	在 HTTP/1.1 里，对于登录页面这样的简单场景，可以使用**定时轮询或者长轮询**的方式实现**服务器推送**的效果。

对于客户端和服务端之间需要频繁交互的复杂场景，比如网页游戏，都不太友好，因此我们需要使用支持全双工的 WebSocket 协议。