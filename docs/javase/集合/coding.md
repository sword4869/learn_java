
```java
ArrayList<Integer> list = new ArrayList<>();
list.add(1);        // add，而不是append
```

```java
ArrayList<Integer> list = new ArrayList<>();
int size = list.size();     // method

int[] a = {1,2,3};
int length = a.length;      // attribute
```