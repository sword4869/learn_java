
---

## 初始化

```java
List<String> list = new ArrayList<>(){{
    add("a");
    add("b");
    remove(0);
}};

HashMap<String, Integer> priority_map = new HashMap<>(){{
    put("(", 0);
    put("+", 1);
}};
```