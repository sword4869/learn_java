test:
  name: bootstrap-dev.yml
  nameBootstrap: bootstrap-dev.yml
  nameLocal: bootstrap-dev.yml
  bootstrap-dev: true

server:
  port: 8081
spring:
  application:
    name: userservice # 服务名称
  cloud:
    nacos:
      server-addr: 192.168.150.3:8848   # nacos服务器地址
      config:
        file-extension: yml   # 必须改，因为默认是properties
